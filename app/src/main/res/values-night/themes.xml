<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The app uses its own bright gradient palette in both system appearance modes. -->
    <style name="Theme.AndroidApp1" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:navigationBarColor">@color/deep_purple</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorAccent">@color/deep_purple</item>
    </style>
</resources>
